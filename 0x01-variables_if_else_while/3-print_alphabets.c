#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char e;

	char f;

	e = 'a';
	f = 'A';
	while
		(e <= 'z') {
			putchar(e);
			e++;
		}
	while
		(f <= 'z') {
			putchar(f);
			f++;
		}
	putchar('\n');
	return (0);
}
