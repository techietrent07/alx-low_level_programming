#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char t;

	int u;

	t = 'a';
	u = 0;
	while
		(u < 10) {
			putchar(u + '0');
			u++;
		}
	while
		(t <= 'f') {
			putchar(t);
			t++;
		}
	putchar('\n');
	return (0);
}
